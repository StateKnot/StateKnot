---
// Copyright 2026 StateKnot contributors
// SPDX-License-Identifier: Apache-2.0

import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout
  title="运维 PostgreSQL 持久化 Provider。"
  description="已实现的 Provider 面向 PostgreSQL 16 与 17，提供 Checksum 锁定迁移、安全传输默认值、有界连接池和 Fail-closed 恢复。"
  section="PostgreSQL Provider"
  locale="zh-CN"
>
  <aside class="docs-callout" aria-labelledby="provider-boundary-title">
    <h2 id="provider-boundary-title">Pre-alpha 运维边界</h2>
    <p>
      Provider 已经真实实现、具有大量测试，并进入精确版本
      <code>0.1.0-alpha.1</code> 公共预览，但仍假设使用可信的 Server-side Pool。角色隔离的
      Server Procedure、通用 Retention、Restore Qualification 与 Failover 仍是发布阻断项；公开
      Run/Result 集成与 Provider-native Agent Graph 已经在该 Store 之上实现。第一批
      Model Adapter 也位于该 Store Boundary 之上。
    </p>
  </aside>

  <section aria-labelledby="sequence-title">
    <h2 id="sequence-title">显式迁移，然后连接</h2>
    <p>
      迁移凭据与运行时凭据必须分离。迁移是部署动作，不是每次应用启动时隐式发生的副作用。
    </p>
    <div class="docs-code" data-code-block>
      <div class="docs-code-head">
        <span>Rust · 当前 pre-alpha API</span>
        <button
          class="copy-button affordance"
          type="button"
          data-copy-button
          data-copy-target="zh-postgres-connect-code"
        >
          <span data-copy-label aria-live="polite">复制</span>
        </button>
      </div>
      <pre
        id="zh-postgres-connect-code"
        tabindex="0"><code>use stateknot_store_postgres::&#123;PostgresStore, PostgresStoreOptions&#125;;

let options = PostgresStoreOptions::default();
PostgresStore::migrate_database(&migration_url, options.clone()).await?;
let store = PostgresStore::connect(&runtime_url, options).await?;
store.health_check().await?;</code></pre>
    </div>
    <p>
      Schema 缺失、版本过新或过旧、迁移不完整、Checksum
      不一致都会导致启动拒绝。不能捕获该错误后继续运行部分兼容的数据库。
    </p>
  </section>

  <section aria-labelledby="configuration-title">
    <h2 id="configuration-title">使用统一、已校验的启动配置</h2>
    <p>
      <code>PostgresStoreConfig</code> 提供封闭的环境变量契约和生产安全的 Builder。生产环境默认保持
      TLS 完整验证并禁止隐式迁移；启用生产自动迁移时， 必须提供独立的 <code
        >STATEKNOT_MIGRATION_DATABASE_URL</code
      >。错误和
      <code>Debug</code> 输出都会对连接 URL 脱敏。
    </p>
    <div class="docs-code" data-code-block>
      <div class="docs-code-head">
        <span>Rust · 环境变量配置</span>
        <button
          class="copy-button affordance"
          type="button"
          data-copy-button
          data-copy-target="zh-postgres-environment-code"
        >
          <span data-copy-label aria-live="polite">复制</span>
        </button>
      </div>
      <pre
        id="zh-postgres-environment-code"
        tabindex="0"><code>use stateknot_store_postgres::&#123;PostgresStore, PostgresStoreConfig&#125;;

let config = PostgresStoreConfig::from_env()?;
let store = PostgresStore::connect_config(config).await?;</code></pre>
    </div>
    <p>
      <code>DATABASE_URL</code> 为必填项；<code>STATEKNOT_AUTO_MIGRATE</code> 与
      <code>STATEKNOT_DEV_MODE</code> 是可选的封闭布尔值。生产迁移使用独立权限 URL。仅在可信本地数据库上，<code
        >STATEKNOT_DEV_MODE=true</code
      > 才会启用迁移、 关闭 TLS 并把连接池限制为四个连接；等价的显式入口是 <code
        >PostgresStore::connect_development(...)</code
      >。
    </p>
  </section>

  <section aria-labelledby="defaults-title">
    <h2 id="defaults-title">安全且有界的默认值</h2>
    <dl class="docs-definition-list">
      <div>
        <dt>传输</dt><dd>
          默认验证 TLS 证书链与主机名，URL Query 无法静默削弱该策略。
        </dd>
      </div>
      <div>
        <dt>连接池</dt><dd>
          1–16 个连接、5 秒获取超时、10 分钟 Idle Retirement 与 30
          分钟最大连接寿命。
        </dd>
      </div>
      <div>
        <dt>事务</dt><dd>
          5 秒 Lock Timeout 与 15 秒 Statement
          Timeout；无效或无界的时序关系会在连接阶段 Fail Closed。
        </dd>
      </div>
      <div>
        <dt>所有权</dt><dd>
          Run Lease 为 30 秒，最大续租 Horizon 为 5 分钟；Outbox Network Attempt
          使用固定、不可续租的 1 分钟 Lease。
        </dd>
      </div>
    </dl>
  </section>

  <section aria-labelledby="graph-registry-title">
    <h2 id="graph-registry-title">把 Graph Version 当作不可变 Release</h2>
    <p>
      Migration 13 增加 Tenant-scoped Compiled Graph Registry。Key 由精确 Graph
      Owner、Name 与 Version 组成；Canonical Bytes 和 Definition Digest 不能在该
      Key 下变化。Run 依赖其 Reference 前必须先注册 Graph。
    </p>
    <div class="docs-code" data-code-block>
      <div class="docs-code-head">
        <span>Rust · 幂等 Graph Registration</span>
        <button
          class="copy-button affordance"
          type="button"
          data-copy-button
          data-copy-target="zh-postgres-register-graph-code"
        >
          <span data-copy-label aria-live="polite">复制</span>
        </button>
      </div>
      <pre
        id="zh-postgres-register-graph-code"
        tabindex="0"><code>let outcome = store
    .register_graph_definition(tenant_id, compiled_graph)
    .await?;</code></pre>
    </div>
    <p>
      相同重试会收敛到原注册；同一版本下的不同 Bytes 会返回冲突。每次 Load
      都会重新编译并验证 Canonical Bytes、冗余 Key Column、Digest 与 Tenant
      Binding；Claimed Recovery 会在精确 Live Fence 下隔离缺失或损坏的 Pinned
      Evidence。
    </p>
  </section>

  <section aria-labelledby="admission-title">
    <h2 id="admission-title">禁止暴露只初始化一半的 Agent Run</h2>
    <p>
      Migration 15 增加不可变 <code>agent_admissions</code>。<code
        >DurableAgentAdmission</code
      > 在一个 Transaction 内提交已认证 Intent、数据库时间、Active Lifecycle、 Sequence-one
      Event、Superstep-zero Checkpoint、Graph Anchor 与 Scheduler Projection。精确
      Retry 会恢复并重校验原 Commit；Changed Intent 会产生 Conflict。
    </p>
    <p>
      Scheduler-ready Index 现在要求 Checkpoint，因此低层未初始化 Row 无法被
      Claim。可信 Ingress、Idempotency、Rollout 与敏感数据要求见<a
        href="/zh/docs/admission/">持久化准入指南</a
      >。
    </p>
    <p>
      Migration 16 增加 Tenant-scoped 持久化 Submission Key。Key Mapping 与
      Admission 同时 Commit，不保存 Raw Key；相同 Logical Retry 可以使用新的
      Candidate ID，Changed Content 或同 Run 的第二个 Key 会被拒绝。详见<a
        href="/zh/docs/runs/">Run/Result 指南</a
      >。
    </p>
  </section>

  <section aria-labelledby="tls-title">
    <h2 id="tls-title">不要随意削弱 TLS</h2>
    <p>
      生产环境应使用 <code>VerifyFull</code>。<code>RequireEncryption</code> 只加密但不认证
      Server，属于明确降级；<code>Disabled</code> 仅适用于可信本地 Socket 或隔离测试网络。
    </p>
  </section>

  <section aria-labelledby="artifact-registry-title">
    <h2 id="artifact-registry-title">把 Artifact 注册为不可变 Evidence</h2>
    <p>
      Migration 18 增加 Tenant-qualified <code>artifacts</code> 与 <code
        >artifact_parents</code
      >。每条记录会绑定 Canonical Reference Bytes/Digest、Content
      Length/Digest、 Origin Run/Event、Same-tenant Lineage、Idempotency Key
      与私有、Provider-neutral Locator。Exact Retry 会收敛；Changed Metadata
      会产生 Conflict，每次 Load 都会重新校验完整 Projection。
    </p>
    <p>
      Object I/O 位于 Registry Transaction 之外。<a href="/zh/docs/artifacts/"
        >Artifact 持久化指南</a
      >定义私有 Conditional Publication、Authorization-before-lookup、完整
      Integrity Verification、Egress Policy 与 Cleanup 责任。
    </p>
  </section>

  <section aria-labelledby="terminal-model-binding-title">
    <h2 id="terminal-model-binding-title">
      绑定已知 Model Failure，但不削弱 Barrier
    </h2>
    <p>
      Migration 19 允许 Pending Node Result 绑定状态为 <code>committed</code> 或
      <code>failed</code> 的精确 Terminal Model Revision。Composite Foreign Key 仍固定
      Tenant、Run、Invocation、Revision、Digest、Status 与 Journal Anchor； Prepared/Executing
      Revision 仍不可绑定，并继续阻塞 Checkpoint 推进。
    </p>
    <p>
      Provider-native Graph 只把它用于确定性、有界 Output Repair：Response Phase
      的
      <code>response.malformed</code> Failure 必须携带精确 Usage，才能成为已消费 Evidence。PostgreSQL
      16/17 Matrix 会验证已填充 v18→v19 Upgrade、Legacy Constraint 拒绝、Failed-binding
      Reload 与 Successor Barrier Commit。
    </p>
  </section>

  <section aria-labelledby="permissions-title">
    <h2 id="permissions-title">分离数据库角色</h2>
    <p>
      Migration Role 需要创建所属 Schema 与迁移表的权限。Runtime Role
      只需要文档声明的表与 Projection 操作权限；不应获得
      DDL，也不应获得修改或删除不可变 Journal、Checkpoint、Invocation
      Revision、Attempt、Result、Graph Definition 或 Consumption History
      的权限。
    </p>
    <p>
      精确 Grant Surface 维护在仓库的<a
        href="https://github.com/StateKnot/StateKnot/blob/main/docs/postgresql-provider.md"
        >Provider 运维文档</a
      >中。请按实际部署版本审核，不要复制旧文章中的 Grant。
    </p>
  </section>

  <section aria-labelledby="qualification-title">
    <h2 id="qualification-title">验证范围与当前限制</h2>
    <p>
      CI 会分别在 PostgreSQL 16 与 17 上执行强制的 Provider、Runtime 与
      Artifact-store 集成测试，覆盖迁移、Lost
      ACK、Fencing、损坏拒绝、并发、Rollback Injection、Outbox Recovery、Durable
      Wait、Quarantine、不可变 Graph Registration/Recovery、Ready-node Planning
      与延迟重试调度、Lifecycle Commit、可恢复 Model/Tool 执行、Fairness
      Reservation / Retention、原子 Agent Admission、Canonical Parallel Sibling
      Commit、有序 Read-only Tool Wave 与 Write Barrier、持久化
      Ingress-key/Run-result 校验和加权 Cross-tenant Agent Loop 执行。Model
      Binding Evidence 还覆盖已填充的 Migration 18→19、 Committed-only
      Constraint 拒绝、精确 Failed-revision Recovery 与 Successor Barrier
      Release。Artifact Evidence 还覆盖已填充的 Migration 17→18、Schema
      Refusal、不可变 Registration、私有 Object Publication、
      Authorization-first Lookup、Multipart Ingestion 与完整 Digest 校验。
    </p>
    <p>
      独立的 <code>pg_dump</code>/<code>pg_restore</code> 演练验证已消费子 Join 的完整快照与图恢复执行；另有物理
      <code>pg_basebackup</code> 加 WAL 归档演练，在命名恢复点恢复同一路径并排除之后的写入。两者都不等于完整灾备或同步备库故障切换验收。
    </p>
    <p>
      正式生产发布仍需要角色隔离、通用
      Retention/GC、参考规模的故障切换与恢复演练、Stale-race
      验证、Role-separated Service Deployment 与 Live-workload Qualification。
    </p>
  </section>
</DocsLayout>
